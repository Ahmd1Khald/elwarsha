import 'package:flutter/material.dart';

class WinchReceiveRequests extends StatelessWidget {
  const WinchReceiveRequests({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
